# ReportFlow
Приложение для автоматической генерации красивых PDF отчетов из CSV файлов.

## ✅ Что будет реализовано:
1. **Полная структура проекта** с разделением на два микросервиса
2. **Валидация входных данных** (проверка расширения файла, существования путей)
3. **Обработка ошибок** с логированием на всех уровнях
4. **Callback механизм** - Python API уведомляет .NET API о завершении обработки
5. **Асинхронная обработка** задач в Python (не блокирует запрос)
6. **Красивая генерация PDF** с использованием ReportLab (стили, цвета, форматирование)
7. **Docker Compose** для легкого развертывания всей инфраструктуры
8. **Документация** с примерами использования
9. **Тестовые данные** для проверки работы системы

## 🏗️ Архитектура
Проект состоит из двух микросервисов:
1. **Client API (.NET 8)** - принимает файлы от пользователей, управляет задачами
2. **Python API (FastAPI)** - обрабатывает CSV файлы и генерирует PDF отчеты

Архитектура проекта:
ReportFlow/
├── Client/                    # .NET Web API
│   ├── Controllers/
│   │   └── TasksController.cs
│   ├── Data/
│   │   └── AppDbContext.cs
│   ├── Models/
│   │   └── ReportTask.cs
│   ├── Services/
│   │   └── IPythonApiClient.cs
│   ├── Program.cs
│   ├── appsettings.json
│   ├── Client.csproj
│   └── Dockerfile
├── PythonApi/   версия пети              # Python FastAPI
│   ├── main.py
│   ├── config.py
│   ├── requirements.txt
│   ├── services/
│   │   └── report_generator.py
│   ├── models/
│   │   └── task_models.py
│   └── Dockerfile
├── docker-compose.yml
├── .gitignore
├── test_data.csv
└── README.md

PythonApi/ версия саши
├── main.py                    # FastAPI приложение, эндпоинты
├── config.py                  # Настройки из .env
├── requirements.txt           # Зависимости
├── Dockerfile
├── .env
├── services/
│   ├── __init__.py
│   ├── report_generator.py    # Генерация PDF
│   ├── file_service.py        # Работа с файлами
│   └── callback_service.py    # Уведомление .NET API
├── models/
│   ├── __init__.py
│   └── task_models.py         # Pydantic модели
├── utils/
│   ├── __init__.py
│   └── logger.py              # Логирование
├── raw/                       # 🗑 "Грязные" файлы (исходные CSV)
│   └── .gitkeep
├── processed/                 # ✨ "Чистые" файлы (готовые PDF)
│   └── .gitkeep
└── tests/
    └── test_api.py
## Ожидаемый ход программы: 
┌─────────────────────────────────────────────────────────────────────────┐
│                              КЛИЕНТ                                     │
│                    (Браузер, Postman, curl)                             │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             │ 1. POST /api/tasks/upload (CSV файл)
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     CLIENT API (.NET 8)                                 │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ TasksController.cs                                               │   │
│  │  • Принимает файл                                                │   │
│  │  • Сохраняет в /shared/uploads/{taskId}.csv                      │   │
│  │  • Создает запись в БД (статус: Pending)                         │   │
│  │  • Вызывает Python API через PythonApiClient                     │   │
│  │  • Обновляет статус в БД (Processing)                            │   │
│  │  • Возвращает taskId клиенту                                     │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              │ 2. POST http://python-api:8000/process   │
│                              │    {taskId, inputPath, callbackUrl}      │
└──────────────────────────────┼──────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    PYTHON API (FastAPI)                                 │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ main.py                                                          │   │
│  │  • Принимает задачу                                              │   │
│  │  • Запускает background task                                     │   │
│  │  • Возвращает "accepted" сразу                                   │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              ▼                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ services/report_generator.py                                     │   │
│  │  • Читает CSV из /shared/uploads/{taskId}.csv                    │   │
│  │  • Генерирует PDF в /shared/results/{taskId}.pdf                 │   │
│  │  • Использует ReportLab для красивого форматирования             │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              │ 3. PUT callbackUrl (статус: Ready)       │
│                              │    {status, resultFilePath}              │
└──────────────────────────────┼──────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     CLIENT API (.NET 8)                                 │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ TasksController.cs (PUT /api/tasks/{id}/status)                  │   │
│  │  • Обновляет статус в БД (Ready)                                 │   │
│  │  • Сохраняет путь к PDF файлу                                    │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
                               │ 4. GET /api/tasks/{id}/download
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              КЛИЕНТ                                     │
│              Получает готовый PDF файл                                  │
└─────────────────────────────────────────────────────────────────────────┘

## 🚀 Быстрый старт
### Требования
- Docker и Docker Compose
- Git
### Установка и запуск
```bash
# Клонируем репозиторий
git clone https://github.com/G4sTt/ReportFlow.git
cd ReportFlow
# Запускаем все сервисы
docker-compose up --build
